menuconfig DRIVERS_HBRIDGE
	bool "HBridge Driver"
	default n
	depends on BOARD_HAS_PWM
	help
	  Enable support for H-Bridge motor drivers with PWM speed control
	  and GPIO direction control. Supports multiple instances with
	  dual channels each.

